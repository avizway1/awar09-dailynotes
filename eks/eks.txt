
Kubernetes : K8s : 

Google built for its internal systems : Project initial name is BORG.. Renamed it to OMEGA to scale their massive infrastructure.. 

In 2014, Goolgle handed it it CNCF as an Open Source Project, and named it as K8s..

CNCF : Cloud Native Computing Foundation : 
	--> Graduated
	--> Incubated

K8s Architecture : 

1. Control Plane : Brain of K8s
2. Data Plane : Hands of K8s


1. CONTROL PLANE COMPONENTS

1.1 kube-apiserver / API Server:
--> This is the front door of K8s. This is the only point of contact to the couster for us.
--> If you want to deploy a new workload, you run "kubectl apply" and talk to api to create workload.
--> It validated the confoiguration first (pod, deployment, service)

1.2 etcd:
--> etcd contains all the information about the k8s cluster and workload.
--> Data stores as key-value pairs (tags)
--> Treat it as k8s Database
--> it stores current and desired state of the cluster.
--> if etcd is gone, the cluster state is lost.

1.3 kube-scheduler: 
--> kube-scheduler looks for availbale worker node to run our workload based on node's resources (i.e; cpu / memory).
--> This wont run the pod, it will picks the best node to run our workload. 

1.4 kube-controller-manager: 
--> Act as Supervisor, This monitors the ETCD and ensure our appliation's current state matches the desired state.
--> If we ask for 3 replicas, and only 2 are running... Then this send instructions to scheduler to pick a node and then it creates a replica.
--> This runs background loops to etcd and make sure desired state and actual state matches.

1.5 Cloud Controller Manager:
--> Connects the k8s to the cloud resources for utilising resources i.e; LB, Roles, VPC..


2. DATA PLANE / WORKER NODE : 

2.1 kubelet : 
--> Kubelet is an AGENT runs in every worker node.
--> It listens to the control plane and ensure that the containers described in the pod specifications are running and healthy.?
--> If container goes down, the kubelet restart it locally, automatically.

2.2 Kubeproxy:
--> This is a networking component, that runs on Every Node. 
--> This assigns IPs to every pod created on Worker nodes.
--> Its network proxy that runs on each node. THis handles network communications inside and outside of our cluster, THis ensures traffic reaches the right contianer.

2.3 Container Runtime: 
--> Responsible for pulling images and running containers.
--> K8s uses cri-o and containerd as runtime. (it used to support docker earlier)
--> kubelet asks container runtime to pull the image and start the container inside the pod. 


pod : Its the smaller deployable unit in K8s. A pod is a wrapper around Container. 
Generally one pod cotnains one Container. Pod is not limited to one container.

A pod can cotnains multiple containers also.. (second container called as sidecar cotnainer)

=================

EKS : Elastic Kubernetes Service : This is AWS fully managed k8s service.
AWS takes care about the CONTROL PLANE Components.
For HA, FT of CONTROL PLANE, AWS Maintains multiple Copies across 3 AZs (almost 6 copies).. 
We dont have access to Control plane nodes. AWS manages these. 

Amazon EC2 Self-managed Node Group : We must ensure the instances are patched and scaled properly, and we have full control over the instances.
--> We manually need to provision, configurae and manage ec2 instances as worker nodes.
--> We need to handle the scaling, OS Updates and security patches.
--> AWS uses bottlerocket ami / al2023 ami. We need to configure these instances.

* Amazon EC2 managed node group: Amazon ECS will manage patching and scaling on your behalf while giving you configurability about the types of instances. Great for more advanced workloads.
--> AWS automatically provision, configure and manage ec2 instances for worker nodes.
--> We need to handle the scaling.
--> We have to choose right sized ec2 instances.

EKS Auto Mode : AWS automatically pickls the required compute resources.

AWS Fargate : serveless, No nodes to manage.. Can directly deploy pods.

======================

D: 04/08/2026


1. AWS CLI Tools must be installed and configured. (https://aws.amazon.com/cli/)
2. Install kubectl in your laptop. (https://kubernetes.io/docs/tasks/tools/)
3. Install eksctl : https://docs.aws.amazon.com/eks/latest/eksctl/installation.html


EKS has 2 types of support option
standard support: 0.10$/Hour
Extended Support: 0.50$/Hour


eksctl create cluster --name ekswithavinash --version 1.35 --region ap-south-1 --zones=ap-south-1a,ap-south-1b --nodegroup-name mycustomng --nodes 2 --node-type c7i-flex.large --node-ami-family=AmazonLinux2023 --managed

--nodes-min 2 --nodes-max 4 --full-ecr-access --external-dns-access

============

imperative Approach :  relies entirely on executing direct commands in our terminal. Fast to create/change/manage resources. Hard to track / manage. 

---

Declarative Approach : Writing a manifest file that act as a blueprint. We can apply and it does the job. 


============

eksctl create cluster -f cluster.yaml

apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: ekswithavinash
  region: ap-south-1
  version: "1.35"

availabilityZones:
  - ap-south-1a
  - ap-south-1b

managedNodeGroups:
  - name: mycustomng
    instanceType: c7i-flex.large
    desiredCapacity: 2
    amiFamily: AmazonLinux2023


==========


oidc : openID Conect : Its an open identity authentication protocol build on top of the OAuth 2.0 framework.

Enable OIDC provider: This oidc helps to connect aws user/components to eks components i.e; service accounts.

eksctl utils associate-iam-oidc-provider --region ap-south-1 --cluster ekswithavinash --approve


========


eksctl autoimatically updates the kubectl with currently created custers context/info. 

kubeconfig file locates in user's home directory: 
~/.kube/config

c:/users/administrator/.kube/config (windows)

--> To update your eks config in new device..

aws eks update-kubeconfig --region ap-south-1 --name ekswithavinash


kubectl get nodes			--> list the worker nodes
kubectl get pods

==========================

apiVersion: v1 (Core group)
Supported components: pods, service, namespace, configmaps and secrets.
Most of the k8s fundamental objects do not requie group name in their manifest files. 

apiVersion: apps/v1 (Named Group)
Supported components: Deployments, Daemonsets, replicasets

meta-data: data about the data.. Data related to pod.. name of pod.. label you want to add to pod.. 


pod : Its the smaller deployable unit in K8s. A pod is a wrapper around Container. 
Generally one pod cotnains one Container. Pod is not limited to one container.


spec: specifications - We need to define desired state and behaviour of the pod.
1. containers list
2. port numbers
3. volumes
4. resource limits

kubectl get pods

kubectl apply -f 01-pod.yaml --dry-run=server		--> dryrun

kubectl apply -f 01-pod.yaml

kubectl describe pod myfirstpod

kubectl get pods -o wide							--> Shows IP address and node info

kubectl logs myfirstpod

kubectl logs -f myfirstpod

kubectl delete pod myfirstpod

kubectl logs myfirstpod --previous					--> Shows logs from crashed pods
kubectl logs myfirstpod -p

kubectl get events									--> shows all the events occured

==================

selector: Defines how the replicaset identifies which pod is responsible for. 

"matchlabel" looks for the pods that have the label

This is crucial to manage. 
Replicaset managesthe pods that matches the selector. 

===================

problem with pod: Due to any reason, if our pod deleted, its gone. New pod wont create automatically.

Solution: replicaset

Replicaset: This will ensure our application is running with given number of identical pods. If any pod deleted, it creates a new pod automatically. 


kubectl get rs
kubectl get replicasets

kubectl delete pods myfirstrs-5wf87 myfirstrs-psjsb myfirstrs-r2t5f

kubectl delete rs myfirstrs


eksctl delete cluster --name ekswithavinash 

==================================================================

D: 05/08/2026


kubectl exec -it mynginx-57db5769ff-5mq5g -- /bin/bash

nginx/1.31.3		--> current version
nginx/1.30.4 		--> Want to change it


Replicaset: This will ensure our application is running with given number of identical pods. If any pod deleted, it creates a new pod automatically. 

If you want to change your application/nginx version, you need to delete the old replicaset and recreate with updated version. 

Problem: We cannot upgrade to newer version. Need to delete and recreate again. 

-----------------

Deployment : This is K8s highest level of abstraction that manages the replicasets and pods. 

if our nginx version is 1.30.4
you decided to update it to 1.31.3
Deployment ensures that, upgradation takes place without any downtime.
If we required, we can rollback to previous version (if anything goes wrong).


rolling update: 
max surge : 2
min unavailable: 1

kubectl exec -it mynginx-57db5769ff-86g4m -- /bin/bash

kubectl rollout history deployment/mynginx

kubectl rollout undo deployment/mynginx						--> undo the latest deployment

kubectl rollout undo deployment/mynginx	 --to-revision=3	--> undo the latest deployment

kubectl rollout status deployment/mynginx

kubectl rollout restart deployment/mynginx

=======================

Resource Limiting: 

kube-scheduler uses this value to find a cluster node with enough room to host the pod. If no node has enough capacity, pod wont be scheduled.

Requests : Guaranteed Minimum : The CPU or memory K8s guarantees to allocate to a cotnainer. 

Limits: Hard Ceiling : The absolute maximum amount of CPU or memory a container is permissted to consume at runtime. This prevents a single container from utilising alot of resources. 


memory: Measured in bytes.. 
Megabytes : M
Mebibytes : Mi

CPU measured in cores or millicores.. 
1000m --> 1 vCPU
250m --> 25% of a CORE (0.25 CPU)

kubectl describe pod mynginx-withlimit-8d6b995c8-28zw2

kubectl top pods
kubectl top pods -A

kubectl top nodes


====================================

Service : Service helps to distribute the load to multiple pods. We need a Kubernetes Service because Pod IP addresses are unstable and temporary.

==> Service always use selector option (key and value of the pods)

We have 3 types of services.

1. ClusterIP (Default)
--> Works only internally, within the cluster.
--> This creates a stable IP address, but IT works only within the cluster. 
--> Treat it like "Internal office intercom system"

kubectl get svc

kubectl describe svc cip-service

TO test the output locally in our laptop: 

kubectl port-forward svc/cip-service 8080:80

----------

D: 06/08/2026

2. NodePort Service : Nodeport picks a static port from range to expose our application. 

NodePort has specific range of ports to map: 30,000 - 32,767
We can choose manually, if we want to.
Service will be delivered to outside using Node/Host IP address. 
** We need to open the port at SG of Ec2 instance level. 

--------

3. LoadBalancer

This links to aws Loadbalancer, But it uses Classic Load Balancer (AWS Stopped supporint)
For quick tests, we use this.
CCM - Cloud Controller manager - Helps to interact with AWS and Create the Load Balancer.

-----------

4. Headless Service : If you set clusterIP: None, It creates a headless service, We use this witl stateful applications.

5. Externalname : Instead of routing traffic to pods, it maps a shortcut name in our cluster with External DNS access, We need to integrate Route53 DNS name. * We dont use much in EKS.

=============================== 


Namespace : Helps to maange our workloads with Logical isolations.

Problem: same label; uses by multiple teams, this can cause work overwrites.
Problem: One team's workload is using more cluster resources than they really required.

default	: THis is the default namespace picked by our workload. If we dont specify namespace in manifest, this will be picked.

kube-system : K8s internal infrastructure critical workload management resides here. DO not modify / delete / deploy regular workload here.

kube-node-lease : This act as health heartbeat tracker. Manages node heartbeat to track node availability.

kube-public : This namespace is readably by anyoone, even without authentication.


kubectl get pods			--> picks the default namespace and shows the pods

kubectl get pods -A			--> List the pods from all the namespaces

kubectl create ns myapp

kubectl get pods -n myapp

kubectl config set-context --current --namespace=myapp			--> setting defualt ns as myapp, no need to pass "-n myapp" along with all the commands.


alias kgp='kubectl get pods'
alias kgn='kubectl get nodes'
alias kgs='kubectl get svc'

Addressing problem 2 :

limitrange on Namespace: We can apply max resources we can allocate to containers. 








