

Monolithic Architecture : Binds all software components together into a single, unified codebase. 
--> All or Nothing.. As it deployed as one single unit.
--> We choose one programing language and we develop the entire application.
--> Most of the cases, we need to scale the entire application.
--> Single centralised database for entire application.


Microservices Architecture: THis breaks the application into a collection of smaller, independent servcies that communicate over a network.
--> Each service is completely independent.
--> We can choose required programing languages per module. (frontend --> python, cart --> java, rating --> postgre, user management --> mysql) ==> polyglot.
--> we can scale services independently.. (more servers --> frontend, less servers --> cart)

Amazon : frontned, recommendation, cart, reviews, ratings, payment, address storage..

==================

D: 24/07/2026

Physical Server : Full Isolation, One OS per Server, Minutes to boot, Less portability, hard for scalability.. 
Resource UsageL 10-15% Usage.. 1 app per server.. 

VM / Virtual Machiene : Full Seperate OS per VM.. Multiple VMs one server.. 30 sec - 1 min boot time.. 60-80% resource utilisation.. 10-20 VMs per Server - 10-20 apps.. 

Containers : Process level isolation.. Shared kernel.. MBs in Size.. 1-5 Seconds to Boot.. 90%+ utilisation.. 
100s of containers per server.. Very high portability.. 


It works on my machiene --> is eliminated with containers. 
Fast setup 
Dependency isolation..


2008 --> dotCloud solomon hykes.. 
2013 --> Opencourced it as Docker.. v0.1 released. 
2015 --> OCI (Open container Initiative) is formed to setup Industry standards for containers. 

podman
containerd
CRI-O


sudo dnf install docker -y

sudo systemctl start docker
sudo systemctl enable docker
sudo systemctl status docker
sudo chmod -aG docker $USER
newgrp docker

docker images				--> Shows the images available to run
docker ps					--> Shows the running containers
docker ps -a				--> all containers including stopped / killed

docker pull nginx			--> Pull nginx latest image from docker hub to local laptop

docker search nginx
docker search nginx --limit 5
docker search --filter is-official=true nginx


docker run nginx			--> basic Run. Run in Foregound

docker run -d nginx			--> run docker in detached mode

docker rm <container-id> / <container-name>		--> Remove the cotnainer

docker rmi <image-name> / <repo name>			--> Remove the image

docker run mysql				--> As image not availbale, it pulled the image and failed when running. required env variables missing. 


docker run -d --name mysql-aviz -e MYSQL_ROOT_PASSWORD=s3cret8899 mysql:8.4

docker run -d --name mysql-env --env-file ./mysqlenv.txt mysql

mysqlenv.txt
MYSQL_ROOT_PASSWORD=s3cret8899


docker stop mysql-env			--> sends the SIGTERM (graceful) signal
docker kill	mysql-env			--> sends SIGKILL signal

docker pause <container-id>
docker unpause <container-id>

===============================================

D: 27/07/2026


docker inspect <container-id/name>

docker stats

TO restrict container's CPU and Memory: 

docker run -d --name mynginx --memory=128m --cpus=0.25 nginx

--memory=256m
--memory=512m
--cpus=1
--cpus=0.25

docker inspect --format='{{.HostConfig.Memory}}' mynginx
docker inspect --format='{{.HostConfig.NanoCpus}}' mynginx

------------

Port Mapping: We can map a host port to container and we can deliver output to outside world.

Make sure to open Host defined ports at Instance SG.

docker run -d -p <host-post>:<container-port> nginx

docker run -d --name mynginx1 -p 80:80 nginx

docker run -d --name mynginx2 -p 8080:80 nginx

docker run -d --name mynginx3 -p 8888:80 nginx

ss -tulnp		==> To verify port status

======================

Restart polocies : We can control what happens when a container stops or the host reboots.

docker run -d --restart=always --name mynginx5 -p 80:80


always				: Always restart, Including on host reboot
unless-stopped		: Similar to always, but not if manually stopped.
no					: Never restart

---------------------

docker logs <container-id>/<container-name>		--> Shows the logs

docker logs -f <container-id>					--> *Shows the logs in realtime / live logs. 

docker logs --tail 10 <container-id>			--> Most recent 10 logs

docker events								--> Stream live events

docker top <container-id>					--> Shows the processes inside a container.

---------------------

copy files from/to container.


docker cp <container-name/id>:/usr/share/nginx/html/index.html ./		--> Copy from container to local

docker cp ./index.html <container-name/id>:/usr/share/nginx/html/index.html		--> copy to container from local

docker diff <container-name/id>					--> THis shows the file system changes

----------------

Scenario: We have image in instance-1, THe same image, we need it in instance-2. But, instance 2 dont have internet to get it from dockerhub..???

docker save -o mynginxshare.tar nginx:latest			==> THis creates a tar/zip of image. We can copy/port anywhere

docker load -i mynginxshare.tar						--> Import tar as a image

---------------

docker networking: 

docker network ls				--> THis lists all the networks

bridge : default network.. All containers get their own virtual network.. 
host : container shares the host machiene network directly..
None : No network at all


docker network create myisolatednw

docker run -dit --name mynginx --network myisolatednw nginx

docker run -dit --name c1 --network myisolatednw alpine
docker run -dit --name c2 --network myisolatednw alpine


Bridge Mode: 
--> Bridge mode will have its own IPs inside the docker network.
--> It required port mapping (-p 80:80 (or) -p 8888:80)
--> THis is more isolated and secured
--> Works on Linux, MacOs, Windows
--> This mode has a NAT component, that helps to resolve container's IP. One additional HOP involved in traffic flow. 
--> best for Enterprise grade / Multi COntianer applications.

HOST Mode : 
--> Share host's IP and Port.
--> Port mappinf not required.
--> Less isolated compared to bridge, as it uses host network.
--> Works only in Linux OS.
--> Slightly faster comapred to bridge, as no NAT involved.
--> We can use this for System level tools i.e; monitoring agents.

-----------

docker container prune				--> Clear all the stopped containers

docker image prune					--> Clears dangling images (untagged images)
docker image prune -a				--> Clears all unused images.

docker network prune				--> Clears unused network
docker volume prune					--> clears unused volumes

docker system prune -a				--> Removes unused images, containers, nw
docker system prune --volumes

docker rm -f $(docker ps -aq)		--> Use with caution** It will stop and remove all running and stopped containers


====================================

D: 28/07/2026


docker volume path: /var/lib/docker/volumes

docker volume ls					--> List the volumes

docker volume create my_data		--> Create a volumes

docker volume inspect my_data

==> mount point of volume: /var/lib/docker/volumes/my_data/_data


Named Volume: Create a volume first and use it. 
Ananymous VOlume : Create avolume while creating a container, Random id generates.

Named VOlume:
docker run -d --name mysqldb -v my_data:/var/lib/mysql -e MYSQL_ROOT_PASSWORD=s3cret8899 mysql:latest

Ananymous VOlume:
docker run -d --name mysqldb1 -v /var/lib/mysql -e MYSQL_ROOT_PASSWORD=s3cret8899 mysql:latest

----

Bind Mount : we can have files in our host machiene/laptop, We can mount it to Container. 

docker run -d --name nginx1 -p 80:80 -v /home/ec2-user/webcontent:/usr/share/nginx/html nginx:latest
docker run -d --name nginx2 -p 8080:80 -v /home/ec2-user/webcontent:/usr/share/nginx/html nginx:latest
docker run -d --name nginx3 -p 8888:80 -v /home/ec2-user/webcontent:/usr/share/nginx/html nginx:latest

Read only Bind Mount: When someone connect to container and try to modify, it wont allow.. 
docker run -d --name nginx3 -p 8888:80 -v /home/ec2-user/webcontent:/usr/share/nginx/html:ro,z nginx:latest

---

Another way to map a volume to a cotnainer.. 

docker run -d -p 80:80 --mount source=my_data,target=/var/lib/mysql,readonly mysql:latest



==========================

FROM		: Sets the "base image". THis must be the first instruction in Dockerfile.

FROM alpine
FROM ubuntu:latest
FROM node:24
FROM python:3.13.14-trixie
FROM amazonlinux

----

RUN			: Executes the commands during the image build process.

RUN dnf update && dnf install nginx -y

* Best practice is to have less layers, so combine multiple commands into single layer.

----

WORKDIR		: Sets the working directory for all subsequent commands. (RUN, COPY)

WORKDIR /app
WORKDIR /usr/share/nginx/html

----

COPY : COpies the files or directories from host to container.

COPY . .
COPY src/ /app

COPY <source-path> <container-path>

----

ADD : similar to copy, but it supports "zip files". Comonly we use COPY over ADD option.

ADD mydata.tar.gz /app
ADD mydata.tar.gz .

---


CMD : sets the default command to run when the container starts. CMD gets ovverwridden if you pass arguments while you are executing "docker run" command.

CMD ["nginx", "-g","daemon off;"]


FROM amazonlinux:latest
CMD["echo","hello from AVINASH"]		--> We build and image name is "mytest"

NOT PASSING ANY ARGUMENT: 
docker run mytest		--> OUTPUT: "hello from AVINASH"


PASSING AN ARGUMENT WHILE RUNNING:

docker run mytest date						--> OUTPUT date/"Jul 28 2026"
docker run mytest "Hello from ANUDEEP"		--> OUTPUT: Hello from ANUDEEP 

---

ENTRYPOINT: Defines a command that always runs using ENTRYPOINT, Even if you pass any arguments when running "docker run".

ENTRYPOINT["echo","hello from AVINASH"]


FROM amazonlinux:latest
ENTRYPOINT["echo","hello from AVINASH"]		--> We build and image name is "mytest"

NOT PASSING ANY ARGUMENT: 
docker run mytest		--> OUTPUT: "hello from AVINASH"


PASSING AN ARGUMENT WHILE RUNNING:

docker run mytest date						--> OUTPUT: hello from AVINASH date
docker run mytest "Hello from ANUDEEP"		--> OUTPUT: hello from AVINASH Hello from ANUDEEP

--> IF We want we can combine both


FROM python:3.12
ENTRYPOINT["python"]
CMD["app.py"]					--> We build this and got an image with "python-demo"

docker run python-demo			--> python app.py	
docker run python-demo db.py	--> python db.py	


------

EXPOSE 	: Document whoch port container is listening on. (THis Does not actually publish the port, We still need -p when running our containers)

EXPOSE 80
EXPOSE 5000
EXPOSE 3000

-----

USER	: sets the user for running the container. It is highly recommend to run as "non-root" user.


----

ENV		: Sets the environment varaibles.

ENV APP_ENV=prod
ENV NAME=mydemoapp

----

VOLUME	: Create a mount point for shared data or persistent data.

VOLUME /var/lib/mysql

----

LABEL		: Adds metadata to the image (version, description, maintainer)

LABEL maintener="avizway@gmail.com" version="1.1"

----

SHELL 		: changes the default shell used.

SHELL ["/bin/bash"]

ARG			: Define arguments during the build process, not at runtime.

HEALTHCHECK	: Inform docker to verify the container health, to identify, container is still running or not.

STOPSIGNAL

==============================

.dockerignore

node_modules
npm-debug.log
.git
.env




FROM node:22

WORKDIR /usr/src/app

COPY . .

RUN npm ci --only=production

COPY . .

EXPOSE 3000

CMD["node","server.js"]


================


docker build -t docker-exam .


================

D: 29/07/2026

Microservices (Go, Rust): Under 50 MB.
Web Apps (Node, Python): 100 MB – 400 MB.
Heavy Frameworks (Java, AI/ML): 500 MB – 1 GB+.


Multistage: 

docker build -t docker-exam:multi .


docker login


docker tag docker-exam:multi avizway/docker-exam:multi

docker push avizway/docker-exam:multi

docker buildx build --platform linux/amd64,linux/arm64 -t avizway/docker-exam:multios --push .

docker buildx build --platform linux/amd64,linux/arm64 -t avizway/docker-exam:multios --push .

docker buildx build --platform linux/amd64,linux/arm64 -t docker-exam:multios .


docker pull avizway/docker-exam:multios


=============

ECR: similar service as DockerHub. But its AWS Native container registry service.. 

--> Create a repo in ECR

--> login to ECR Repo : 
aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 655700896650.dkr.ecr.ap-south-1.amazonaws.com

--> If image not build, build it

--> Add required tags to the local image

docker tag avizway/docker-exam:multios 655700896650.dkr.ecr.ap-south-1.amazonaws.com/docker-exam:multios

--> push the image to ECR

docker push 655700896650.dkr.ecr.ap-south-1.amazonaws.com/docker-exam:multios


==========

Docker compose helps to run multiple containers at a time using single YAML file. 

sudo mkdir -p /usr/libexec/docker/cli-plugins

sudo curl -SL https://github.com/docker/compose/releases/latest/download/docker-compose-linux-$(uname -m) -o /usr/libexec/docker/cli-plugins/docker-compose

sudo chmod +x /usr/libexec/docker/cli-plugins/docker-compose


docker compose version
docker compose up 
docker compose up -d
docker compose logs
docker compose restart


==========================

services:
  db: 
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: avinash
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes: 
      - pgdata:/var/lib/postgresql/

  adminer:
    image: adminer:latest
    ports:
      - "8080:8080"
    depends_on:
      - db

volumes:
  pgdata:
  
========================










