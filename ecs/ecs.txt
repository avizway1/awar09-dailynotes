

Orchestrator: 
K8s -> Kubernetes
ECS --> Elastic Container Service

--> High Availability and Self healing
--> Elastic Auto Scaling

I need 5 copies of my application.. 
Manual Container management : We need to create copies manually, if anything is down. 
With Orchestrator : it automatically moves to healthy state, by creating new copies.

Manual Management is hard to manage the traffic. 
With Orchestrator : it automatically provisns new containers.

==================

ECS : Elastic Container Service : 

AWS Fargate : Serverless compute provider.. Pay per vCPU/Memory per seconds.. Host not required so NO OS customisations.. you don't think about creating or managing servers. Great for most common workloads.

EC2 Launch Type : We manage ec2 instances, Our containers runs inside the ec2 instances.. Pay for the ec2 instance running hours.. Slow to scale application.. 
	--> managed instances: Amazon ECS will manage patching and scaling on your behalf while giving you configurability about the types of instances. Great for more advanced workloads.
	--> Self-managed instances : you must ensure the instances are patched and scaled properly, and you have full control over the instances.

ECS Anywhere : We can use our own hardware.. We can use ECS to orchestrate our workload.. 


--> Container Insights with enhanced observability : Provides detailed health and performance metrics at task and container level in addition to aggregated metrics at cluster and service level. Enables easier drill downs for faster problem isolation and troubleshooting.

Container Insights : Provides aggregated metrics at cluster and service level. You can run deep dive analysis with logs insights analytics.


Docker = Container
ECS = Task
K8s = Pod


We wan to crate a task / container.. How much CPU you give, How much memory.. Where is your Image.? Access to any other servcies.?

Ans : Task definition : This contains required settings for our task... 

----

Task Definition:

Name: 
Launch type: fargate
Task Size : CPU and Memory for one container

Task role : If our container required to access any AWS services, We can add it here..
Task Execution Role : ecsTaskExecutionRole : Basic/Essential permissions to run the container and pulling th eimage from ECR.. 

Container details: 
	Name: 
	Essential container: True : if container dies, It kills the task. 
	Imae URI: Pick from ECR (Use Digest / SHA ID)
	
Storage: 
--> By default, all containers gets the Ephemeral storage.. Size: 20 GB.. it will be available till container is live. 

----------

Turn on ECS Exec : Use ECS Exec to run interactive commands in containers running on EC2, Fargate or Managed Instances. 
--> This feature requires a task role with appropriate permissions. (ssmmessages)
--> Can only be enabled when launching new tasks, not for existing ones.
--> make sure to add these policy to the ECS task role.

-----------

ECS CLuster : 
Task Definition : 
Task.!! 


Task : Task is an individual copy of our appliation, If task stops, it wont create automatically. 
service : Combination of tasks that deliver same application.. Also, We can integrate with ELB & ASG.

===============================

D: 31/07/2026

1. Create 3 SG in CustomVPC 

1. ELB Sg : Deliver application via Load Balancer								: sg-06dc40b0720cf44e1
2. Task SG : Use this with ECS tasks.. It need to deliver content to ELB-SG		: sg-0c1d0dde1ffbea7d5
3. EFS SG : Use with EFS service.. it need to deliver content to TASK-SG		: sg-09b370b3b1448be66

---

2. Create an EFS and Create an Access Point.

3. Update the Task Definition with EFS as storage. 
	--> Under volumes, add a volume, choose "EFS --> access point --> adv settings --> enable "transit Encry"
	
---

If our workload/tasks are in Private subnet.. To Pull the image from ECR, To store temp layers on s3, To write logs on CW logs, We need VPC Endpoints..

ECR DKR Endpoint (Docker Registry)
ECR API Endpoint (For Authentication)
Logs (CLoudwatch) ENdpoint
S3 Gateway Endpoint (Image layers)

--> Endpoints need Security Group.. Open https to entireVPC, and use with Endpoints.. 




















